# Local Server Mysql Configuration
# spring.datasource.url=jdbc:mysql://127.0.0.1:3306/test_marketplace?verifyServerCertificate=false&useSSL=false&requireSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# user for mysql local server
# spring.datasource.username=root

# password to mysql local server
# spring.datasource.password=Mysqlroot

# Server Mysql Configuration
spring.datasource.url=jdbc:mysql://coms-309-vb-05.cs.iastate.edu:3306/COMS309_VB05_ISUMARKETPLACE?verifyServerCertificate=false&useSSL=false&requireSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# user for mysql server VB_05
spring.datasource.username=VB_05

# password to mysql server
spring.datasource.password=VB_05_User

# this is the port your web server will run on
server.port=8080

# Hibernate SQL Dialect
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# JPA
# This tells jdbc to create the tables if they do not already exist
# Defaults to none, in which case you must make the tables to match the Entity owners
# Run the 'src/main/resources/db/mysql/data.sql' script in MySQL workbench or CLI to insert dummy data
spring.jpa.hibernate.ddl-auto=create

spring.jpa.show-sql=true
logging.file=application.log

# Logging
logging.level.org.springframework=DEBUG

# Swagger-UI
server.contextPath=/swagger2-demo

# Configure SMTP Properties
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=coms309.2020@gmail.com
spring.mail.password=Coms3092020!
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000




